{
  "name": "audit-log-dashboard",
  "version": "1.0.0",
  "description": "Full-stack Audit Log Dashboard with bulk upload and search, sort, filtering, pagination.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "generate-data": "node backend/generate_logs.js",
    "start:backend": "npm run dev --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
